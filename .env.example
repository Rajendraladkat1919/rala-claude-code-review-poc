# Copy to .env and adjust. POC uses mock weather by default (no API keys).
FLASK_ENV=development
SECRET_KEY=change-me-in-production
# true = use built-in mock data (default). false reserved for a future HTTP provider.
USE_MOCK_WEATHER=true
